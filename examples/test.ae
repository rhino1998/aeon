package main

var memo [20]int

#init
func main() {
  for i := 0; i < len(memo); i++ {
    memo[i] = -1
    print(fib(i))
  }
}

func fib(n int) int {
  if n <= 1 {
    return n
  }
  if memo[n] != -1 {
    return memo[n]
  }
  ret := fib(n-1) + fib(n-2)
  memo[n] = ret
  return ret
}
