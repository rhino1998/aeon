package main

type intIntMap map[int]int

type A *B

type B int

func main() string {
  // test comment
	//
	z := 0
	for i := 0; i < 10; i++ {
		z+=h(i)
	}

  return z
}


func f(a ()) int {}

func g(y int) int{
	return 3
}

func h(x int) int {
	if x <= 0 {
		return 0
	} else if x == 1 {
		return 1
	}
	return h(x-1) + h(x-2)
}

func j() () {}

func k() []map[string]string {}
