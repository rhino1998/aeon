package main

type intIntMap map[int]int

type A *B

type B int

extern func print(string)

extern func add(a int, b int) int

var x = "hello"

var y (string,int)

const g = "HJJJ"

func init() {
  print(x)
  x = "there"
  return
}

func main() {
  // test comment
	//

  for i := 0; i < 5; i++ {
    print(x)
  }

  y.0 = "test"
  y.1 = 5

  x = "world"

  print(x)
  print(g)
  print(y.0)

  add(1,2)

  return
}
