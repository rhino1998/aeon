package main

type intIntMap map[int]int

type A *B

type B [3]int

extern func itoa(int) string

extern func print(string)

extern func printInt(int)

extern func print3(a string, b string, c string)

extern func add(a int, b int) int

var d any

type D string

type C struct {
	A string
	B int
}

func (c *C) C() D {
	c.A = "9"
	return D(c.A)
}

func whatever() {
  y := D("hello")
  d = &y
  d = nil
	for i := 0; i < 16; i++ {
		print(itoa(f(i)))
	}
  return
}

func f(a int) int {
	if a <= 0 {
		return 0
	} else if a == 1 {
		return 1
	}

	return f(a-1) + f(a-2)
}

#update
func hello() {
	print("HELLO")
	return
}
