package main

func fib(n int) int {
  if n <= 0 {
    return 0
  }
  if n == 1 {
    return 1
  }
  return fib(n-1) + fib(n-2)
}

#init
func main() {
  assert(fib(0) == 0)
  assert(fib(1) == 1)
  assert(fib(10) == 55)
  print(fib(20))
}
