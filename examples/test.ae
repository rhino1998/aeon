package main

extern func itoa(x int) string

#init
func main() {
  z := make([]int, 100)
  for i := 0; i < 400000; i++ {
    z = make([]int, 100)
  }

  y := make([]int, 0, 1)
  for i := 0; i < 5120; i++ {
    y = append(y, i)
  }

  for i := 0; i < 40000; i++ {
    z = make([]int, i)
  }

  for i := 0; i < len(y); i++ {
    if y[i] != i {
      print((itoa(i)) + " !=  " + itoa(y[i]))
    }
  }
}
