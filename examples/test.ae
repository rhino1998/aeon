package main

type intIntMap map[int]int

type A *B

type B int

extern func print(string)

extern func add(a int, b int) int

func init() {

  return
}

func main() {
  // test comment
	//

  for i := 0; i < 5; i++ {
    print("HELLO")
  }
  add(1,2)

  return
}
