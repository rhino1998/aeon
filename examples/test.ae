package main

type intIntMap map[int]int

type A *B

type B int

func main() {
  var x int = 5
  var y = "hello"
  var z = true

  w := 7
  w += 9
  w = 10

  x++
  y+="a"

  var l (int, int, int)

  if (x == 5) {
    return
  } else if x == 2 {
    return
  } else {
    return
  }

  for var i B = 5; i < 2; i++ {
    g := i
  }

  // test comment

  return
}


func f(a ()) int {}

func g() (int, int) {}

func j() () {}

func k() []map[string]string {}
