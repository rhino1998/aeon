package main

extern func itoa(a int) string

extern func print(a string)

#init
func main() {
  var x []int
  for i := 0; i < 1000; i++ {
    x = append(x, i)
  }

  print(itoa(len(x)))
  print(itoa(cap(x)))
}
---
1000
1024
